<?xml version="1.0" encoding="utf-8" ?>
<uranium:UraniumContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
                            xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                            x:Class="AppPallet.View.ChequeView"
                            Title="Cheques"
                            xmlns:viewModels="clr-namespace:AppPallet.ViewModel"
                            xmlns:uranium="http://schemas.enisn-projects.io/dotnet/maui/uraniumui"
                            xmlns:material="http://schemas.enisn-projects.io/dotnet/maui/uraniumui/material"
                            xmlns:fa="clr-namespace:UraniumUI.Icons.FontAwesome;assembly=UraniumUI.Icons.FontAwesome"
                            xmlns:dg="clr-namespace:Maui.DataGrid;assembly=Maui.DataGrid">


    <ScrollView>
        <Grid RowDefinitions="Auto,Auto">
            <Grid Padding="20" ColumnSpacing="20" ColumnDefinitions="0.8*,*,0.5*">
                <Border Stroke="green" StrokeThickness="3" HeightRequest="50" WidthRequest="100" Grid.Column="0">
                    <Label Text="Verde" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
                <Border Stroke="red" StrokeThickness="3" HeightRequest="50" WidthRequest="100" Grid.Column="1">
                    <Label Text="Rojo" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
                <Border Stroke="black" StrokeThickness="3" HeightRequest="50" WidthRequest="100" Grid.Column="2">
                    <Label Text="Negro" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>
            </Grid>

            <Grid Grid.Row="1" ColumnDefinitions="0.5*,*,0.5*">
                <Border Stroke="Silver" StrokeThickness="3" HeightRequest="50" WidthRequest="100" Grid.Column="0">
                    <Label Text="Plata" HorizontalOptions="Center" VerticalOptions="Center"/>
                </Border>

                <Border Stroke="Silver" StrokeThickness="3" Grid.Column="1">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="10"/>
                    </Border.StrokeShape>
                    <dg:DataGrid ItemsSource="{Binding ListaCheques}"            
                                 IsRefreshing="{Binding IsBusy}" 
                                 PaginationEnabled="True"
                                 Style="{StaticResource TablaComun}"
                                 SelectedItem="{Binding ChequeSeleccionado, Mode=TwoWay}"
                                 PageSize="20" 
                                 PageNumber="1"
                                 SelectionMode="Single" 
                                 BorderColor="Transparent">
                        <dg:DataGrid.Columns>
                            <dg:DataGridColumn Title="Proveedor">
                                <dg:DataGridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Label Text="{Binding Proveedor}" 
                                               HorizontalOptions="Center" 
                                               VerticalOptions="Center" HorizontalTextAlignment="Center" Padding="0,10,0,0"/>
                                    </DataTemplate>
                                </dg:DataGridColumn.CellTemplate>
                            </dg:DataGridColumn>
                            <dg:DataGridColumn Title="Tipo">
                                <dg:DataGridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Label Text="{Binding Tipo}" 
                                               HorizontalOptions="Center" 
                                               VerticalOptions="Center" Padding="0,10,0,0"/>
                                    </DataTemplate>
                                </dg:DataGridColumn.CellTemplate>
                            </dg:DataGridColumn>
                            <dg:DataGridColumn Title="N° de cheque">
                                <dg:DataGridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Label Text="{Binding NumCheque}" 
                                               HorizontalOptions="Center" 
                                               VerticalOptions="Center" Padding="0,10,0,0"/>
                                    </DataTemplate>
                                </dg:DataGridColumn.CellTemplate>
                            </dg:DataGridColumn>
                            <dg:DataGridColumn Title="Monto">
                                <dg:DataGridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Label Text="{Binding Monto, StringFormat='${0:N2}'}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                    </DataTemplate>
                                </dg:DataGridColumn.CellTemplate>
                            </dg:DataGridColumn>
                            <dg:DataGridColumn Title="Fecha de Emision">
                                <dg:DataGridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Label Text="{Binding FechaEmision, StringFormat='{0:dd/MM/yyyy}'}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                    </DataTemplate>
                                </dg:DataGridColumn.CellTemplate>
                            </dg:DataGridColumn>
                            <dg:DataGridColumn Title="Fecha de Pago">
                                <dg:DataGridColumn.CellTemplate>
                                    <DataTemplate>
                                        <Label Text="{Binding FechaPago, StringFormat='{0:dd/MM/yyyy}'}"
                                               HorizontalOptions="Center"
                                               VerticalOptions="Center"/>
                                    </DataTemplate>
                                </dg:DataGridColumn.CellTemplate>
                            </dg:DataGridColumn>
                            <dg:DataGridColumn Title="Estado" PropertyName="Estado" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>
                        </dg:DataGrid.Columns>
                    </dg:DataGrid>
                </Border>


                <VerticalStackLayout Grid.Column="2" Spacing="10" >
                    
                    <Button StyleClass="FilledButton" BackgroundColor="#46e06f" Text="Agregar cheque" Command="{Binding MostrarPopupChequeCommand}"  WidthRequest="180"/>

                    <Button StyleClass="OutlinedButton" BorderColor="#98c47e" TextColor="#98c47e" Text="Modificar cheque" Command="{Binding MostrarPopupChequeModificarCommand}"  WidthRequest="180"/>

                </VerticalStackLayout>
                
            </Grid>
        </Grid>
    </ScrollView>

</uranium:UraniumContentPage>